# Pepta handoff — 2026-08-04 (evening)

Continuation handoff for a fresh session. Previous handoff: `docs/HANDOFF-2026-07-26.md`.
Read `~/.claude/.../memory/MEMORY.md` too — the durable gotchas live there. This doc is
the point-in-time state.

---

## 0. The one-paragraph situation

Pepta (GLP-1/peptide tracker, RN/Expo frontend + Node/Mongo backend on Render +
RevenueCat) has real TestFlight users and a live App Store listing. **1.0.6 (31) was
uploaded to App Store Connect tonight (Aug 4 ~20:00)** and is the build Nick intends to
swap onto the 1.0.6 version page that is waiting for approval (replacing build 30).
**The annual price change $39.99 → $59.99 fires Aug 5 in ASC** — the app is fully
price-change-proof (all price strings derive from live StoreKit). A complete replacement
App Store screenshot set (9 panels) is rendered and ready in
`marketing/optimization-final-set/final/`. Everything is committed and pushed through
`dd1150c` on `main`.

---

## 1. Repo + environment ground rules (violate these and you will lose hours)

- **Work in `/Users/roadto1million/Developer/Pepta`.** The Desktop copy
  (`Desktop/Programing/Repos/BoltzmanLab/Pepta`) is STALE, iCloud-synced (fileproviderd
  wedges reads → hour-long hangs), and has `STALE-COPY-DO-NOT-USE.md` at its root.
- `Pepta/` itself is the git repo (remote `https://github.com/Nmilien34/Pepta.git`,
  branch `main`); the BoltzmanLab parent is NOT a repo.
- `marketing/` is **gitignored** — screenshot assets live on disk only.
- Monorepo: `pepta-frontend/` (RN/Expo), `pepta-backend/` (Express/Mongo),
  `shared/` (zod schemas). npm workspaces hoist to the Pepta root.
- macOS has no `timeout`; `$?` after a pipe lies. `pod`/`xcodebuild` need
  `export LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8`.
- Frontend verify loop: `npx tsc --noEmit -p tsconfig.json`, `npx vitest run`,
  `npx eslint src` — all from `pepta-frontend/`. Current state: **tsc 0 errors,
  90 files / 620 tests green, eslint 0 errors + 5 pre-existing exhaustive-deps
  warnings** (BottomSheet, QuickLogSheet, WheelPicker, PeptaDataContext:718 unused
  disable, CycleSetupScreen — all pre-existing, leave them).
- Backend verify: from `pepta-backend/`: tsc clean, 41 files / 235 tests green.
- `scripts/preflight-release.sh` (frontend) MUST pass before any archive — it checks
  pod parity, prod env, tsc, lint (rules-of-hooks is load-bearing), tests.

## 2. Release / App Store Connect state (as of Aug 4 ~20:00)

| Version | Build | Status |
|---|---|---|
| 1.0.3 | 24 | Was live; superseded |
| 1.0.4 | 26 | **APPROVED by Apple** → its train is CLOSED (this forced 1.0.5) |
| 1.0.5 | 27, 28, 29 | Uploaded; 29 is the best 1.0.5 (centered badges + double-reveal fix). One of these may have been submitted/pulled — Nick manages that |
| 1.0.6 | 30 | Uploaded Aug 4; attached to the 1.0.6 version page **waiting for approval** |
| 1.0.6 | **31** | **Uploaded Aug 4 tonight — the intended swap-in.** Nick's next ASC action: 1.0.6 page → Remove from Review (if in review) → attach 31 → resubmit |

- Version sources (Info.plist CFBundleShortVersionString/CFBundleVersion, pbxproj
  MARKETING_VERSION/CURRENT_PROJECT_VERSION, app.config.js version/buildNumber) all read
  **1.0.6 (31)**. Bump ALL THREE places every time; Info.plist is hardcoded (agvtool is
  not enough). An APPROVED version closes its train — new builds then need a marketing
  version bump. A build swap on a pending version requires the SAME marketing version
  (that's why tonight's build is 1.0.6 (31), not 1.0.7).
- Archive flow (all proven this session, ~6-8 min each):
  `cd pepta-frontend/ios && xcodebuild -workspace Pepta.xcworkspace -scheme Pepta
  -configuration Release -destination 'generic/platform=iOS' -archivePath
  "$HOME/Library/Developer/Xcode/Archives/$(date +%Y-%m-%d)/Pepta-<ver>-<build>.xcarchive"
  -allowProvisioningUpdates archive`, then `xcodebuild -exportArchive` with
  `ios/ExportOptions-AppStoreConnect.plist` (destination=upload; auth is Xcode's own
  session — there is NO ASC API key on this machine, so ASC state can never be queried
  headlessly; ask Nick).
- Bundle verification before upload: python search of `main.jsbundle` — Hermes stores
  strings containing non-ASCII (em dashes etc.) as **UTF-16-LE**; check both encodings.
  Template-literal strings won't appear whole — search their static fragments.

## 3. Pricing / trial state (the Aug 5 price change)

- **Aug 5, ASC-scheduled:** `ai.boltzman.pepta.annual` $39.99 → $59.99 (175 territories,
  existing subscribers preserved at $39.99). Monthly `$9.99` unchanged
  (`ai.boltzman.pepta.monthly` / `.monthly.trial`). **Both plans carry a 3-day free
  trial** — monthly via the trial product, annual via an introductory offer added
  directly on the annual product Aug 3 (free / 3 days / 175 countries). RevenueCat
  offering unchanged (`$rc_monthly` / `$rc_annual` on the current offering).
- The old A/B experiment expa9f87848e1 is STOPPED and rolled out. No arm constraints
  remain, but the trial gating below still protects against any no-trial state.
- **Everything price-shown derives from live StoreKit** (`paywallPricing.ts`):
  - Savings badge: `Math.round((1 − yearly/(monthly×12))×100)` → **SAVE 50%** at
    $59.99 (conventional rounding is a DELIBERATE Nick decision, twice affirmed —
    do not "fix" it back to floor; the comment at the call site records this).
  - Per-month anchor: `$5.00`/mo with `$59.99/yr` beneath (3.1.2(c): billed total
    also in the legal footer).
  - A loaded product missing numeric price gets NO badge (never a stale constant).
  - `FALLBACK_PRICING` = the ONLY literal prices in the app; renders only while
    packages load (CTA disabled, "Loading App Store plans…" shown); aligned to the
    Aug 5 list; commented as last-resort-will-drift.
  - Trial-end reminder notification reads the PURCHASED package's own priceString
    ($59.99 for a yearly trialist).
  - No screen anywhere shows a plan price to an already-subscribed user (Account
    deep-links to Apple's manage page) — verified twice.

## 4. Paywall behavior (PaywallScreen.tsx + paywallPricing.ts + paywallTimeline.ts)

- Light theme, arrival framing. Yearly ALWAYS preselected (Nick: not up for debate).
- **Package-agnostic trials**: every trial surface derives from a package's own
  `introPrice` + per-package eligibility (`PaywallPackages.trial.{monthly,yearly} =
  {eligible, rawStatus}`), resolved in ONE batched
  `checkTrialOrIntroductoryPriceEligibility` call. `TRIAL_COPY_PERMISSIVE_ON_UNKNOWN
  = true` (UNKNOWN permits copy; only INELIGIBLE suppresses; non-iOS/missing/throw →
  false).
- **Badge priority (Nick, Aug 4 — display only, derived, never hardcoded):**
  1. yearly eligible → yearly wears green "3 days free", monthly none;
  2. yearly ineligible + monthly eligible → badge moves to monthly, yearly reverts
     to SAVE 50% (purple);
  3. neither → SAVE badge only.
  When the trial owns yearly's slot, SAVE relocates to the sub-line:
  "billed yearly · save 50%". Badges are centered over the card (a right-pinned
  float collided with the radio). Green = fiber #34C759 so "free" never reads as
  "selected". Disclosure is untouched by badge choices — CTA/subline always
  describe the SELECTED plan's real trial+price.
- CTAs: trial-eligible selected plan → **"Try today for $0.00"** (duration lives in
  the timeline + subline, NOT the button — Nick's call); otherwise
  "Start my year — $59.99" / "Start my month — $9.99".
- Trial timeline (`paywallTimeline.ts`, pure + tested): Today / Day N−1 reminder /
  Day N charge with computed calendar date; collapses to Today + the FEATURES grid
  when the selected plan has no trial; reminder row omitted for 1-day trials.
- Analytics: `paywall_shown` {variant, defaultSelectedPlan, trialCopyShown
  (= default-selected plan), trialCopyPlans none|monthly|yearly|both — describes
  OFFERS not pixels}; `paywall_offering_debug` per-package (TODO(remove) once
  trial-on-both confirmed healthy); `paywall_dismissed` fires once when app is
  BACKGROUNDED from the wall without purchase ('inactive' excluded — StoreKit sheet);
  `purchase_started` per tap. RC subscriber attributes: `setEmail`/`setDisplayName`
  applied best-effort on identify.

## 5. Onboarding flow (the part most likely to be worked on next)

37 steps in `onboardingFlow.ts` (`ONBOARDING_STEPS`). Tail of the funnel:
`… notifications → crafting → reveal → trialOffer → trialCarousel → paywall → welcomeIn`.
There is NO standalone auth step (merged 2026-07-29; `LEGACY_STEP_MAP` maps saved
'auth' drafts → 'reveal'). Draft key `pepta.onboarding.v2` — pure insertions never
bump it; reorders/removals do.

- **RevealScreen — “payoff-first” (aea11b2, tonight, in build 31).** This is the
  fix for the screen-32 funnel drop:
  - Payoff (goal-path graph draws segment-by-segment with per-segment haptics, flag
    pops, confetti, proof chips) renders for EVERYONE, unconditionally.
  - Footer is a single **"Start today"** ConvoButton for everyone.
  - Signed-out tap → `logRevealClaimTapped()` (once/session) + the **save sheet**
    springs up OVER the still-visible plan (Modal, backdrop fade, spring translateY,
    soft haptic): grabber, "Save your plan", "Your plan is private to you.",
    Google ProviderButton + native AppleAuthenticationButton (guideline 4: system
    artwork only), inline error, Terms/Privacy links. Backdrop tap dismisses back
    to the plan. Sign-in advances via the navigator's auth watcher.
  - Signed-in tap → `onContinue()` directly, no sheet, no event.
  - WHY (memory `pepta-reveal-payoff-first`): the reveal is the funnel's only
    delivered payoff; merging the identity ask into it read as 32 questions →
    identity → pay. GLP-1 privacy makes the ask a real objection. The
    `reveal_claim_tapped` → `af_complete_registration` pair now separates
    "bounced off the payoff" from "refused identity". If the drop moves to the
    sheet, post-paywall auth reopens — but that's a BIG refactor (paywall,
    purchase, completeOnboarding, AccessGate all require a backend user; anonymous
    RC + aliasing is the phantom-customer minefield).
- **Auth-transition memory lives in the navigator's module `flowCache`
  (`wasAuthenticated`)**, NOT a ref — signing in flips AccessGate through its
  authenticated-undecided branch which REMOUNTS the navigator; a ref reset to
  "already signed in" and caused the double-reveal bug (fixed befb07d, in 1.0.5(29)+).
  Seeded with current auth state on the session's first mount so resumed-authed
  users aren't flung past the reveal. Reset in handleComplete.
- **Trial warm-up (32fb5fb):**
  - `TrialOfferScreen` ("One last thing… / We'd love you to try Pepta — 3 days on
    us" / CTA "See my free offer"): staggered entrance, LivingMascot 'gift' pose
    (bob+blink+heartbeat pulse), breathing halo, sparkles. **TRIAL-GATED on mount**:
    fetches getPaywallPackages; announces the PRESELECTED plan's (yearly) duration
    when plans differ, falls back to monthly's; if NEITHER plan has an eligible
    trial (or error, or no user) it calls `onSkipToWall` → silently jumps past the
    carousel to the paywall. The warm-up can never block the funnel or promise
    free days the wall won't show.
  - `TrialCarouselScreen`: 4 mini-device slides (dose countdown w/ pulsing due dot;
    level curve draws itself with the both-ends bezier `Easing.bezier(0.32,0,0.24,1)`;
    weight trend + "Muscle protected" chip spring-pop; symptom chips stagger),
    manual paging only, animated dots, selection haptic per snap, Mascot 'peek'
    pose ducking on page change, persistent "See my free offer" CTA.
  - Mascot poses 'gift' and 'peek' are overlays/crops on the same body in
    `Mascot.tsx` (its own convention; no new character art).
  - `ConvoProgressHeader` / `ConvoGround` exported from ConvoScreen so custom turns
    share the persistent progress fill.
- The paywall has NO back button → control users can't back-navigate into a
  warm-up they skipped.
- Funnel events (funnelEvents.ts): `onboarding_step` fires per rendered step with
  canonical index (in-memory session dedupe); `onboarding_started`/`_completed`
  once-per-install (AsyncStorage); `reveal_claim_tapped` once/session.

## 6. App update prompt (shipped in 1.0.4, LIVE once 1.0.5+ releases)

- Backend: public `GET /app-config/version` (no auth, never 500s) — iTunes Lookup
  with `t=` cache-buster (Apple CDN caches ~3.6h), 1h in-memory TTL, last-known-good
  on failure, `latestVersion: null` = show nothing. Env overrides (ALL optional,
  empty-string tolerated — an empty var must never crash boot):
  `PEPTA_MINIMUM_VERSION`, `PEPTA_FORCE_UPDATE` ("true", case/space tolerant),
  `PEPTA_UPDATE_TITLE/MESSAGE`, `PEPTA_LATEST_VERSION` (escape hatch).
  Bundle id is `ai.boltzman.peptaapp` (hygiene test pins it against app.config.js).
- Frontend: `appUpdate.ts` (numeric semver, 3s timeout, fail-open everywhere) +
  `AppUpdateGate` mounted ONLY inside AccessGate's onboarded MainTabs shell (never
  during onboarding). Soft prompt 72h-throttled (marked at DISPLAY time); hard gate
  built but dormant; never hard-gates a user already at/past known latest.
  Events `update_prompt_shown/_action`. First real firing = when 1.0.6 releases,
  1.0.5 devices should prompt within ~1h.

## 7. Marketing — the new screenshot set (DONE, awaiting ASC upload by Nick)

`marketing/optimization-final-set/` (gitignored; render with `./render.sh` —
headless Chrome → `final/*.png` at exactly **1284×2778** + `thumbs/` 200px checks):

1. `01-know-your-body` — home screen (WITH −/+ steppers: Fiber 1 g, Protein 5 g,
   Water 8 oz, Activity, Today's Log), lilac-cut model right, "Know exactly what's
   in your body." (126px, no eyebrow/sub), hero card "Medication Level 1.42 mg /
   still active right now".
2. `02-library` — eyebrow ANY COMPOUND, "Every compound. One library.", 6 big white
   tags (Semaglutide/Tirzepatide/Retatrutide + BPC-157/TB-500/GHK-Cu — all verified
   loggable), real Library screen (search + Wolverine/GH-support stacks + entries),
   amber "Can't find it? Add your own." (library is 28 compounds + 4 stacks → NO
   count claim vs PeptidePal's 110+). Headline at BOTTOM. GLP-1 variant deleted.
3. `03-every-shot` — Track screen, "Every shot, right on time.", mirror-selfie
   model, hero "NEXT DOSE 3d 9h / reminder set · sites rotate automatically".
4. `04-meal-scan` — ported "Snap it. Log it." (subline re-enabled, em dash → comma).
5. `05-ask-pep` — "Questions? Ask Pep.", real PepChat mirror (greeting, TWO
   grounded exchanges — 1.42 mg levels + "42 g short" protein consistent with the
   set's 112/154), real chips, composer, guy cutout lower-left.
6. `06-reminders` — ported "Daily reminders." + subline.
7. `07-calculator` — "Never do vial math again." bottom headline, FULL phone (Nick
   rejected the top-bleed twice — show the whole device), hero answer card
   "10 mg + 1 mL = 10 units". NOTE: this file needed explicit
   `font-family: var(--f)` on custom blocks — serif fallback appeared without it.
8. `08-quick-log` — ported "Log a shot in one tap."
9. `09-learn` — ported "Don't just track. Learn."

Cutout pipeline (venv in the session scratchpad, may need recreating):
rembg `isnet-general-use`, `post_process_mask=True`, NO alpha-matting (it haloes);
then alpha<60→0, keep largest connected component (scipy), 2px erosion + 1px
gaussian edge. u2net_human_seg amputates held objects — don't use it.
Design-review rhythm with Nick: render → he critiques → iterate (he cares about:
real app fidelity incl. steppers, no rainbow colors, big readable text, nothing
important cropped, no dead space, fewer-but-bigger elements).

Also his ASC-side to-dos flagged repeatedly: replace the stale IAP Review
screenshot ("Start 7-day free trial", $40.00/$9.00 — wrong on all counts), upload
the 9 panels to slots 1–9, possible subtitle change.

## 8. Design hub

- `pepta-frontend/design-lab/` — `onboarding-v2.html` (funnel spec; merged-reveal
  frame annotated REV 3 for the payoff-first change), `trial-warmup.html` (screens
  A/B/C spec; badge note rev'd Aug 3 to slot-priority), `index.html` (app screens).
- RULE (memory `design-from-shipped-components`): mockups port the live RN
  StyleSheets — never freestyle. Palette: primary #7C5CFC, convo tokens in
  `src/components/onboarding/convoTokens.ts`, Hanken Grotesk everywhere.

## 9. Recent commit log (newest first, all pushed)

- `dd1150c` Bump 1.0.6 (31) — payoff-first reveal build
- `aea11b2` Payoff-first reveal: Start today for everyone, auth in slide-up sheet
- `2e52a24` Trial badge priority: one row wears the badge — 1.0.6 (30)
- `6d15270` Savings badge: conventional rounding — SAVE 50%
- `c1d8141` Price-change-proof pricing for Aug 5 $59.99
- `0e76aa4` Bump 1.0.5 (29); `befb07d` double-reveal fix; `d12f00d` centered badges
- `0367664` Package-agnostic trials (both plans) — also: RC setEmail/setDisplayName,
  paywall_dismissed, yearly-trial reminder-price fix
- `380ea7e`/`a174eb0` version bumps; `32fb5fb` trial warm-up sequence;
  `929d520`+`af9aeaf` app update prompt + loop-audit hardening;
  `479ea81` trial badge/eligibility/debug-event; `ead0395`/`98c1120` RC hygiene

## 10. Open tasks (board) + watchpoints

Task board: #1 referral discount frames; #2 ASC offer-code doc + CLI; #3 referral
implementation (blocked on Nick generating codes in ASC; NEVER a hidden SKU);
#4 complimentary-access verification; #6 rehome referral code entry; #8 gate review
prompt on 3rd logged dose; #12 rotate REVENUECAT_SECRET_API_KEY + AppsFlyer dev key.

Watchpoints for the next session:
- **Aug 5**: price change fires. Paywall should show $5.00/mo · $59.99/yr · SAVE 50%
  with zero action. If Nick reports otherwise, check `paywall_offering_debug`.
- **When 1.0.6 releases**: update-prompt goes live for 1.0.5 devices (watch
  `update_prompt_shown`); backend on Render must be deployed (auto-deploys from main).
- **When build 31 has traffic**: read `reveal_claim_tapped` vs
  `af_complete_registration` — low tap-rate = payoff problem; taps without
  registrations = identity refusal (→ post-paywall-auth discussion reopens).
- `paywall_offering_debug` is TODO(remove) once trial-on-both is confirmed healthy.
- Release sim build lives at `/tmp/pepta-sim` (may be gone after reboot); iPhone 17
  Pro sim E705AE6D-F971-4298-B6D0-A577F68BFC85 was used. Animation feel has never
  had a human pass — Nick was offered a driven sim walkthrough repeatedly.

## 11. Working-with-Nick notes

Typos are dense — read for intent. He decides by LOOKING (render, send PNG, iterate).
He overrides "technically correct" for marketing judgment (SAVE 50% vs 49) — record
such reversals in code comments as deliberate. He asks "anything else? then build+
upload" at the end of a work block — bump all three version sources, preflight,
archive, verify bundle strings (both encodings), exportArchive-upload, commit the
bump. He reattaches builds in ASC himself. Codex sometimes works in parallel —
verify ASC build numbers with him before archiving (memory `codex-collab`).
